//example of folder based routing using folder type content
const index= ()=> {
    return (
      <>
        <p>Hello this is about page </p> 
      </>
    );
  };
  export default index; // we can write index ina specific folder component, it will automatically take as /about